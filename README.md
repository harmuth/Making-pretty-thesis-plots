# Making pretty thesis plots
How to use the "latex-font" in ggplot2 plots:
* Install the two fonts.
* Knit the 'Pretty plots.Rmd' file with `knitr`.

Dependent on `ggplot2, gridExtra, kableExtra, extrafont, grid, Hmisc`.
